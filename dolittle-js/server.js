var pushmi_pullyu = require('./lib/base/pushmi_pullyu.js');

var object = new pushmi_pullyu('localhost', 1883, 'Alfred', 'in1', 'out1');
//object.process_loop();

   //start doctor
   //start watchdog for doctor
